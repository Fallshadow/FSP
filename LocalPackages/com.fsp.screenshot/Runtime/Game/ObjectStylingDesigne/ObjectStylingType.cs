namespace fsp.modelshot.Game.ObjectStylingDesigne
{
    public enum ObjectStylingType
    {
        Empty = 0,                                      // 仅仅有一个节点，生成的东西都在这个节点下随意摆放
        RexEditor_Weapon_Library = 1,                   // RexDebug下项目武器库策略
    }
}